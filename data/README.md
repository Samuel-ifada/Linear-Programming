# Data

Place small, shareable input data files here only when they are needed to reproduce repository examples.

Large calculation outputs should stay outside Git or be handled with a separate storage strategy.
